@model Book

<style>
    .card .btn {
        z-index: 1;
    }
    .card:hover
    {
        border-color: #373b3e;
        box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.075), 0 0 8px #373b3e;
    }
</style>


<div class="card bg-dark m-3 rounded-3" style="width: 17.82rem;">
    <img src=@Model.imgUrl class="card-img-top p-3" width="80px" height="320px" alt="BookImage">
    <a asp-controller="Book" asp-action="Details" asp-route-id="@Model.bookId" class="stretched-link"></a>
    <div class="card-body d-flex flex-column h-100 ">
        

        <h5 class="card-title text-white">@Model.title</h5>
        <h6 class="card-subtitle text-muted mb-2" style="color:white!important">@Model.author</h6>

        <p class="card-text text-white" style="overflow-y: auto;">@Model.shortDescritpion</p>
        
        <a  asp-controller="ShoppingCart" asp-action="AddToShoppingCart" asp-route-bookId="@Model.bookId" class="mt-auto btn btn-dark bg-dark customTextColor ">Buy</a>
        

    </div>

    <div class="card-footer d-flex justify-content-center text-white">@Model.publisher</div>
</div>